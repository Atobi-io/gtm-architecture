# GTM Engine — architecture reference (rendered)

Published from the engine repo's `docs/architecture/` by
`skills/gtm-build-shared/publish_architecture.sh`. Do not edit here —
changes land in the engine repo and are synced by the script.

Entry point: https://atobi-io.github.io/gtm-architecture/
